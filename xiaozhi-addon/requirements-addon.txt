# Xiaozhi ESP32 Server - Lightweight requirements for HAOS add-on
# No torch/torchaudio/funasr - uses SherpaASR (sherpa_onnx) + SileroVAD (onnxruntime)

# Core
numpy==1.26.4
websockets==14.2

# Audio
opuslib_next==1.1.5
pydub==0.25.1

# ASR (SherpaASR via sherpa_onnx - no torch needed)
sherpa_onnx==1.12.17

# VAD (SileroVAD uses onnxruntime directly - no torch needed)
onnxruntime>=1.17.0

# LLM
anthropic>=0.49.0
openai==2.8.1

# TTS
edge_tts==7.2.6

# HTTP / WebSocket
httpx==0.28.1
aiohttp>=3.13.3
aiohttp_cors==0.8.1
requests==2.32.5

# Config / Logging
PyYAML>=6.0
ruamel.yaml==0.18.16
loguru==0.7.3

# Auth
PyJWT==2.10.1

# Templates
Jinja2==3.1.6

# Plugins
bs4==0.0.2
cnlunar==0.2.0
chardet==5.2.0

# MCP
mcp==1.22.0

# System
aioconsole==0.8.2
markitdown==0.1.3
psutil==7.1.3
portalocker==3.2.0
PySocks==1.7.1
